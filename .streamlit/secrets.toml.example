# Copie este arquivo para .streamlit/secrets.toml (uso local) ou cole o
# conteudo em App settings > Secrets no Streamlit Cloud.

# "csv" ou "api" (o painel publicado ja usa "api")
DATA_SOURCE = "csv"

# Usado somente quando DATA_SOURCE = "api". Backend real do time:
API_BASE_URL = "https://backend-ml-obesity.onrender.com"
